// import { RECEIVE_PREFERENCE } from '../actions/portfolio_actions';
import {merge} from 'lodash';

const portfolioReducer = (state = {}, action) => {
  Object.freeze(state);
  return null;
};

export default portfolioReducer;